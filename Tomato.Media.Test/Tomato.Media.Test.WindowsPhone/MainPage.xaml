<Page
    x:Class="Tomato.Media.Test.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tomato.Media.Test"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>

        <local:ViewModel x:Name="ViewModel"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
          DataContext="{StaticResource ViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Slider Name="sl_Time" Margin="10" Value="{Binding CurrentTime}" ValueChanged="sl_Time_ValueChanged"/>
        <StackPanel Grid.Row="1">
            <Button Click="Button_Click">Play</Button>
            <Button Click="PauseButton_Click">Pause</Button>
            <Button Click="StopButton_Click">Stop</Button>
            <Button Click="SwitchButton_Click">Switch</Button>
        </StackPanel>
    </Grid>
</Page>